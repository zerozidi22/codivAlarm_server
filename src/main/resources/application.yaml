server:
  address: localhost
  port: 9080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://34.123.72.51:3306/codiv?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    password: silly!@34
    username: silly
  hikari:
    auto-commit: false
    connection-test-query: SELECT 1
    minimum-idle: 10
    maximum-pool-size: 50
    transaction-isolation: TRANSACTION_READ_UNCOMMITTED
    pool-name: pool-kai
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
    open-in-view: false
